# Task list

This file is THE authoritative record of task state. One line per task file in `.tasks/`. List each task as `- NNN [MARKER] Title`. Keep the latest added task at the top.

Whenever a task file's `Status:` line changes, update its line here in the same edit.
